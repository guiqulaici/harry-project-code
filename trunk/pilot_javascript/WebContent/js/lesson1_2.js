/**
 * javascript的数组
 * 定义: var arr = new Array();
 * 属性:
 * 	constructor 返回创建此对象的数组的函数引用
 * 	index
 * 	length 长度
 *  input
 * 方法:
 * 	concat 合并数组
 *  join 把数组
 * */
var arr1 = new Array();
